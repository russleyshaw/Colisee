/* eslint-disable */
//TODO: Enable eslint

//TODO: Expand common docker interface

class Docker {

    /**
     * Builds a docker image
     * @param image_tag
     * @param dockerfile
     * @param callback
     */
    build(image_tag, dockerfile, callback) {
    }

    run(container_tag, image_tag, callback) {
    }

    save(image_tag, filename) {
    }


}